Installation:

	git clone https://github.com/jengmicah/dotvim.git

Create symlinks:

	ln -s ~/.vim/vimrc ~/.vimrc
	ln -s ~/.vim/gvimrc ~/.gvimrc

Swtich to the '~/.vim' directory, and fetch submodules:

	cd ~/.vim
	git submodule init
	git submodule update
